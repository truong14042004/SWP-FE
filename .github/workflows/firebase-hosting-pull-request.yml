# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
        env:
          VITE_API_URL: https://carrerrmap-522918119520.asia-northeast1.run.app
          VITE_GOOGLE_CLIENT_ID: 522918119520-282u6g5hu32k2471m5asjr8oieg5d5b4.apps.googleusercontent.com
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SWP_FE_CAREERMAP_2026_47CA0 }}
          projectId: swp-fe-careermap-2026-47ca0
